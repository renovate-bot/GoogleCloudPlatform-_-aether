(DEFINE_MODULE
  (NAME 'type_error_test')
  (INTENT "Test type error detection")
  (CONTENT
    (DECLARE_CONSTANT
      (NAME 'wrong_type')
      (TYPE INTEGER)
      (VALUE "this is a string not an integer")
      (INTENT "This should cause a type error")
    )
  )
)